using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Verito
{
    class cop
    {
        /*
             vError1.Lbl = label4;
            vError1.Lbl = textBox1;
            vError2.Lbl = label3;
            vError2.Lbl = textBox2;


            DataTable reqtable = DoDatabase.select("select * from formfield where formid=" + formid.ToString());

            foreach (DataRow row in reqtable.Rows)
            {   
                  string field =row["fieldid"].ToString();
               VError ve=  getVE(field);

            }
          private VError getVE(string field)
        {
            foreach (VError  ve in panel2.Controls)
            {
                if (ve.Id.Equals(field))
                {
                    return ve;
                }
            }
            return null;
        }
         */
    }
}
